import styled from 'styled-components';

export const Wrapper = styled.div`
    display: flex;
    flex-direction: column;
    gap: 64px;
    align-items: flex-start;
`;
export const Header = styled.div`
    display: flex;
    flex-direction: column;
    gap: clamp(2.25rem, calc(2.03rem + 1.12vw), 3.19rem);
`;
